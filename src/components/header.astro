---
import { Image } from 'astro:assets'
import imageLogo from '../assets/imgs/logo.webp'
---

<header class="header">
  <a class="containerLeft" id="containerLeft" href="/" aria-label="Ir a inicio">
    <Image src={imageLogo} alt="Logo de CompuAudio" loading="eager" />
    <!-- <p class="siteName" id="siteName" aria-label="Nombre de la empresa">
      Compu<span>Audio Pereira</span>
    </p> -->
  </a>
  <nav class="containerRight" aria-label="Menú principal">
    <ul>
      <li>
        <a href="/">Inicio</a>
      </li>
      <li>
        <a href="/contact">Contacto</a>
      </li>
      <li>
        <a href="/products" class="products">Productos</a>
      </li>
    </ul>
  </nav>
</header>

<style>
  .header {
    text-align: center;
    background-color: #f9f9f9;
    padding: 1rem;
    border-bottom: 1px solid #eaeaea;
    display: flex;
    justify-content: space-between;
    box-shadow: 0 2px 4px 0 rgba(0, 0, 0, 0.1);
    margin-inline: 3em;
  }

  .header .containerLeft {
    display: flex;
    align-items: center;
    /* box-shadow: 6px 7px 4px 0px rgb(0 0 0 / 32%); */
    border-radius: 10px;
    background-color: black;
  }

  .header .containerLeft img {
    width: auto;
    max-width: 80px;
    border-radius: 10px;
    box-sizing: border-box;
  }

  .header .containerLeft::after {
    content: 'CompuAudio Pereira';
    display: none;
    width: 0;
    background: #050505;
    font-size: 2rem;
    transition: width 0.4s ease;
    padding: 10px;
    overflow: hidden;
    white-space: nowrap;
  }

  .header .containerLeft:hover::after {
    width: 100%;
    display: block;
    color: #ffffff;
    animation: expandWidth 0.4s ease forwards;
  }

  @keyframes expandWidth {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }

  /* .header .containerLeft p {
  font-size: 2.5rem;
  font-weight: 700;
  color: #c55600;
}

.header .containerLeft span {
  color: #ffffff;
  padding-right: 20px;
} */

  .containerRight {
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: center;
  }

  .header .containerRight ul {
    display: flex;
    align-items: center;
    flex-direction: row;
    gap: 40px;
    font-size: 1.4rem;
    color: #000000;
  }

  .header .containerRight ul li {
    list-style: none;
  }

  .header .containerRight ul li a {
    text-decoration: none;
    color: #000000;
  }

  .header .containerRight ul li a:not(.products):after {
    content: '';
    display: block;
    width: 0;
    height: 2px;
    background: #c55600;
    transition: width 0.3s;
  }

  .header .containerRight ul li a:not(.products):hover::after {
    width: 100%;
    color: #c55600;
  }

  .header .containerRight ul li .products {
    background-color: #c55600;
    color: #ffffff;
    padding: 0.5rem 1rem;
    border: none;
    border-radius: 5px;
    cursor: pointer;
  }

  .header .containerRight ul li .products:hover {
    background-color: #c55500c5;
    color: #ffffff;
  }

  /* Hover en el contenedor y que aparezca el texto opcion */
  /* .header .containerLeft:hover > p {
  display: block;
  animation: animate 1s ease-in-out forwards;
}


@keyframes animate {
  0% {
    transform: scale(1);
    opacity: 0;
  }
  50% {
    transform: scale(1.1);
    opacity: 1;
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
} */
</style>
